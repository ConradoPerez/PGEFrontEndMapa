<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:mapsui="clr-namespace:Mapsui.UI.Maui;assembly=Mapsui.UI.Maui"
             x:Class="IntegrarMapa.MainPage">

    <AbsoluteLayout>

        <!-- 🌍 Mapa -->
        <mapsui:MapControl x:Name="mapControl"
                           AbsoluteLayout.LayoutFlags="All"
                           AbsoluteLayout.LayoutBounds="0,0,1,1" />

        <!-- 📋 Panel lateral -->
        <Frame AbsoluteLayout.LayoutFlags="PositionProportional"
               AbsoluteLayout.LayoutBounds="1,0.5,250,500"
               BackgroundColor="#f4f4f4"
               CornerRadius="15"
               HasShadow="True"
               Padding="10">

            <!-- Contenedor dinámico que se llenará desde MainPage.xaml.cs -->
            <ContentView x:Name="MenuContainer" />

        </Frame>
    </AbsoluteLayout>
</ContentPage>
